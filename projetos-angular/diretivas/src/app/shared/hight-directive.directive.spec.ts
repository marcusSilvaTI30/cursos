import { HightDirectiveDirective } from './hight-directive.directive';

describe('HightDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HightDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
